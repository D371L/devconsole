# 🚀 Настройка Full-Stack приложения с Backend API

## Структура проекта

```
devconsole/
├── backend/              # Node.js API сервер
│   ├── server.js        # Express сервер
│   └── package.json     # Зависимости backend
├── frontend/            # React приложение (корень проекта)
├── database/
│   └── schema.sql       # SQL схема для PostgreSQL
└── .do/app.yaml         # Конфигурация DigitalOcean
```

## Что было создано

### 1. Backend API (`backend/`)
- **Express сервер** с REST API endpoints
- Подключение к PostgreSQL через `pg`
- CORS для работы с frontend
- Endpoints для:
  - Users (GET, POST, PUT, DELETE)
  - Tasks (GET, POST, PUT, DELETE, комментарии)
  - Projects (GET, POST)
  - Snippets (GET, POST, DELETE)
  - Health check

### 2. API Service (`services/apiService.ts`)
- TypeScript сервис для работы с API
- Методы для всех CRUD операций
- Автоматическое определение URL API

### 3. Обновленный AppContext
- Гибридный режим: API + LocalStorage fallback
- Автоматическая проверка доступности API
- При наличии API - использует его, иначе LocalStorage

### 4. Конфигурация DigitalOcean (`.do/app.yaml`)
- Static Site для frontend
- Web Service для backend API
- PostgreSQL база данных
- Автоматические переменные окружения

## Локальная разработка

### Запуск Backend

```bash
cd backend
npm install
npm start
```

Backend будет доступен на `http://localhost:8080`

### Запуск Frontend

```bash
# В корне проекта
npm install
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

### Переменные окружения для локальной разработки

Создайте `.env.local` в корне проекта:
```env
VITE_API_URL=http://localhost:8080/api
GEMINI_API_KEY=your_key_here
```

Создайте `.env` в папке `backend/`:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/devconsole
PORT=8080
NODE_ENV=development
```

## Деплой на DigitalOcean

### 1. Обновите `.do/app.yaml`

Замените `YOUR_GITHUB_USERNAME/YOUR_REPO_NAME` на реальные значения.

### 2. Инициализация базы данных

После создания приложения в DigitalOcean:

1. Подключитесь к базе данных через DigitalOcean Console
2. Выполните SQL из `database/schema.sql` для создания таблиц

### 3. Импорт конфигурации

1. В DigitalOcean App Platform выберите **Settings** → **App Spec**
2. Скопируйте содержимое `.do/app.yaml`
3. Вставьте и сохраните

DigitalOcean автоматически:
- Создаст Static Site для frontend
- Создаст Web Service для backend API
- Подключит PostgreSQL базу данных
- Настроит переменные окружения

### 4. Переменные окружения

Добавьте в **Settings** → **App-Level Environment Variables**:
- `GEMINI_API_KEY` (Scope: RUN_TIME, Type: SECRET)

Backend автоматически получит:
- `DATABASE_URL` от базы данных
- `PORT=8080`

Frontend автоматически получит:
- `VITE_API_URL` от backend сервиса

## Проверка работы

### Health Check

После деплоя проверьте:
```
https://your-app-url.ondigitalocean.app/api/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

### Логи

- **Backend логи**: Settings → Components → devconsole-api → Logs
- **Frontend логи**: Settings → Components → devconsole-frontend → Logs

## Миграция данных

Если у вас уже есть данные в LocalStorage:

1. Экспортируйте данные из LocalStorage (через DevTools)
2. Используйте SQL INSERT команды для импорта в PostgreSQL
3. Или создайте миграционный скрипт в backend

## Troubleshooting

### Backend не запускается

- Проверьте что `DATABASE_URL` установлен
- Проверьте логи в DigitalOcean Console
- Убедитесь что база данных создана и таблицы инициализированы

### Frontend не может подключиться к API

- Проверьте что `VITE_API_URL` установлен правильно
- Убедитесь что backend доступен (health check)
- Проверьте CORS настройки в backend

### Данные не сохраняются

- Проверьте что API доступен (console в браузере)
- Проверьте логи backend на ошибки
- Убедитесь что база данных подключена

## Архитектура

```
┌─────────────┐      HTTP      ┌──────────────┐      SQL      ┌─────────────┐
│   Frontend  │ ─────────────→ │  Backend API │ ────────────→ │ PostgreSQL  │
│ (Static)    │                │ (Web Service)│               │  Database   │
└─────────────┘                └──────────────┘               └─────────────┘
     React                          Express                         PostgreSQL
```

- **Frontend**: Статический сайт, запрашивает данные через REST API
- **Backend**: REST API сервер, обрабатывает запросы и работает с БД
- **Database**: PostgreSQL хранит все данные приложения

## Стоимость

- Static Site: **$0/месяц** (бесплатно)
- Web Service (basic-xxs): **$5/месяц**
- PostgreSQL (Development): **$12/месяц**

**Итого**: ~$17/месяц для полноценного Full-Stack приложения

## Готово! 🎉

Теперь у вас полноценное Full-Stack приложение с:
- ✅ Backend API на Node.js/Express
- ✅ PostgreSQL база данных
- ✅ React frontend с автоматическим API integration
- ✅ Автоматический деплой на DigitalOcean

