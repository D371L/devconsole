# 🚀 Full-Stack деплой на DigitalOcean

## Что было сделано

✅ **Backend API** - Node.js/Express сервер создан  
✅ **API Service** - TypeScript клиент для frontend  
✅ **AppContext** - обновлен для работы с API + LocalStorage fallback  
✅ **Конфигурация** - `.do/app.yaml` готов для Full-Stack  

## Структура

```
devconsole/
├── backend/              # ← НОВЫЙ Backend API
│   ├── server.js
│   └── package.json
├── services/
│   └── apiService.ts     # ← НОВЫЙ API клиент
├── context/
│   └── AppContext.tsx    # ← ОБНОВЛЕН (API + LocalStorage)
├── .do/
│   └── app.yaml          # ← ОБНОВЛЕН (Full-Stack конфигурация)
└── ...
```

## Деплой на DigitalOcean

### Шаг 1: Обновите репозиторий

```bash
git add .
git commit -m "Add backend API and Full-Stack support"
git push origin main
```

### Шаг 2: Обновите `.do/app.yaml`

Замените `YOUR_GITHUB_USERNAME/YOUR_REPO_NAME` на реальные значения.

### Шаг 3: Импорт конфигурации в DigitalOcean

1. Зайдите в [DigitalOcean App Platform](https://cloud.digitalocean.com/apps)
2. Откройте ваше приложение (или создайте новое)
3. Перейдите в **Settings** → **App Spec**
4. Скопируйте содержимое `.do/app.yaml`
5. Вставьте и сохраните

DigitalOcean автоматически создаст:
- ✅ **Static Site** (frontend)
- ✅ **Web Service** (backend API)
- ✅ **PostgreSQL** (база данных)

### Шаг 4: Переменные окружения

Добавьте в **Settings** → **App-Level Environment Variables**:
- `GEMINI_API_KEY` (Scope: RUN_TIME, Type: SECRET)

Остальные переменные настраиваются автоматически:
- `DATABASE_URL` → от базы данных в backend
- `VITE_API_URL` → от backend в frontend

### Шаг 5: Инициализация базы данных

После создания приложения:

1. Подключитесь к базе данных через DigitalOcean Console
2. Выполните SQL из `database/schema.sql`:
   ```sql
   -- Создание таблиц
   -- Вставка начальных данных
   ```

### Шаг 6: Деплой

После сохранения конфигурации DigitalOcean автоматически:
1. Задеплоит backend API
2. Задеплоит frontend
3. Подключит базу данных

## Проверка

### Health Check Backend

После деплоя проверьте:
```
https://your-app-url.ondigitalocean.app/api/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

### Проверка Frontend

Откройте приложение - оно автоматически:
1. Проверит доступность API
2. Если API доступен - будет использовать его
3. Если нет - использует LocalStorage (fallback)

### Логи

- **Backend**: Settings → Components → devconsole-api → Logs
- **Frontend**: Settings → Components → devconsole-frontend → Logs

## Локальная разработка

### Backend

```bash
cd backend
npm install

# Создайте .env файл:
# DATABASE_URL=postgresql://user:pass@localhost:5432/devconsole
# PORT=8080

npm start
```

### Frontend

```bash
# В корне проекта
npm install

# Создайте .env.local:
# VITE_API_URL=http://localhost:8080/api
# GEMINI_API_KEY=your_key

npm run dev
```

## Как это работает

### При старте приложения:

1. **AppContext** проверяет доступность API через `/health`
2. Если API доступен:
   - Загружает данные из PostgreSQL через API
   - Сохраняет изменения через API
3. Если API недоступен:
   - Использует LocalStorage (fallback)
   - Работает как раньше

### Архитектура:

```
Frontend (React)
    ↓ HTTP запросы
Backend API (Express)
    ↓ SQL запросы
PostgreSQL Database
```

## Важно

- ✅ Backend и Frontend в одном репозитории
- ✅ DigitalOcean автоматически определит source_dir
- ✅ Frontend получает URL API автоматически
- ✅ Backend получает DATABASE_URL автоматически

## Стоимость

- Static Site: **$0/месяц**
- Web Service (basic-xxs): **$5/месяц**
- PostgreSQL: **$12/месяц**

**Итого**: ~$17/месяц

---

Готово! 🎉 Теперь у вас полноценное Full-Stack приложение!

