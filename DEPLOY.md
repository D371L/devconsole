# 🚀 Деплой DevConsole на DigitalOcean App Platform

Пошаговая инструкция для деплоя DevConsole с базой данных PostgreSQL на DigitalOcean.

## 📋 Что будет развернуто

- ✅ **Frontend** - React приложение (статический сайт)
- ✅ **PostgreSQL** - база данных для хранения данных
- ✅ **Автоматический деплой** - при каждом push в main ветку

## 🎯 Быстрый старт (5 минут)

### Шаг 1: Подготовка кода

1. Убедитесь что ваш код находится в GitHub репозитории
2. Все изменения должны быть в ветке `main`

### Шаг 2: Создание приложения в DigitalOcean

1. Зайдите в [DigitalOcean App Platform](https://cloud.digitalocean.com/apps)
2. Нажмите **"Create App"**
3. Выберите **"GitHub"** как источник
4. Авторизуйте доступ к вашему GitHub аккаунту
5. Выберите репозиторий с DevConsole
6. Выберите ветку `main`

### Шаг 3: Настройка компонентов

DigitalOcean автоматически определит структуру проекта. Убедитесь что:

#### Frontend (Static Site)
- **Build Command**: `npm install && npm run build`
- **Output Directory**: `dist`
- **HTTP Port**: оставьте пустым

#### База данных
- В разделе **"Resources"** нажмите **"Add Resource"**
- Выберите **"Database"** → **"PostgreSQL"**
- Выберите план (Development - $12/мес или Production)
- Название: `devconsole-db`

### Шаг 4: Переменные окружения

В разделе **"Settings" → "App-Level Environment Variables"** добавьте:

```
GEMINI_API_KEY = ваш_ключ_от_google_gemini
```

- **Scope**: `RUN_TIME`
- **Type**: `SECRET` (рекомендуется)

### Шаг 5: Деплой

1. Нажмите **"Next"**
2. Проверьте конфигурацию
3. Нажмите **"Create Resources"**
4. Дождитесь завершения деплоя (5-10 минут)

Готово! 🎉 Ваше приложение будет доступно по адресу:
```
https://devconsole-xxxxx.ondigitalocean.app
```

## 🔧 Использование базы данных

### Вариант 1: Только Frontend (LocalStorage)

По умолчанию приложение использует **LocalStorage** браузера. Это означает:
- ✅ Данные хранятся локально в браузере
- ✅ Не требуется настройка базы данных
- ✅ Работает сразу после деплоя

**База данных не обязательна** для работы приложения.

### Вариант 2: С базой данных PostgreSQL

Если вы хотите использовать PostgreSQL для хранения данных:

1. База данных будет создана автоматически
2. Подключитесь к базе через DigitalOcean Console
3. Выполните SQL из файла `database/schema.sql` для создания таблиц
4. В будущем можно создать backend API для работы с БД

**Важно:** Сейчас приложение работает с LocalStorage. Для использования PostgreSQL нужно создать backend API.

## 📊 Структура базы данных

После создания базы данных, выполните SQL из `database/schema.sql`:

- `users` - пользователи системы
- `projects` - проекты
- `tasks` - задачи
- `snippets` - фрагменты кода

## 💰 Стоимость

| Компонент | Стоимость |
|-----------|-----------|
| Static Site | **$0/месяц** (бесплатно) |
| PostgreSQL (Development) | **$12/месяц** |
| PostgreSQL (Production) | **от $15/месяц** |

**Итого:** от $0 до $15+ в месяц (в зависимости от выбора)

## 🔄 Автоматический деплой

После настройки:
- ✅ Каждый push в ветку `main` автоматически запускает деплой
- ✅ Логи можно просматривать в реальном времени
- ✅ Откат к предыдущей версии доступен в один клик

## 🛠 Настройка через .do/app.yaml

Файл `.do/app.yaml` содержит конфигурацию для автоматического создания приложения.

**Важно:** Замените `YOUR_GITHUB_USERNAME/YOUR_REPO_NAME` на реальные значения.

После этого можно импортировать конфигурацию:
1. В App Platform выберите **"Import Configuration"**
2. Укажите путь к `.do/app.yaml`
3. DigitalOcean автоматически создаст все компоненты

## 📝 Переменные окружения

### Обязательные:
- `GEMINI_API_KEY` - API ключ от Google Gemini (для AI функций)

### Автоматические (для базы данных):
- `devconsole-db.DATABASE_URL` - строка подключения к PostgreSQL
- `devconsole-db.DATABASE_HOST`
- `devconsole-db.DATABASE_PORT`
- `devconsole-db.DATABASE_USER`
- `devconsole-db.DATABASE_PASSWORD`
- `devconsole-db.DATABASE_DB`

## 🐛 Troubleshooting

### Проблема: Build fails

**Решение:**
- Проверьте что команда `npm run build` работает локально
- Убедитесь что все зависимости указаны в `package.json`
- Проверьте логи в разделе **Build Logs**

### Проблема: Environment variable не работает

**Решение:**
- Убедитесь что scope установлен как `RUN_TIME`
- Перезапустите приложение после добавления переменных
- Проверьте что переменная доступна через `process.env.GEMINI_API_KEY`

### Проблема: Приложение не открывается

**Решение:**
- Проверьте что build завершился успешно
- Убедитесь что `catchall_document: index.html` настроен
- Проверьте логи в разделе **Runtime Logs**

## 📚 Дополнительные ресурсы

- [DigitalOcean App Platform Docs](https://docs.digitalocean.com/products/app-platform/)
- [PostgreSQL в App Platform](https://docs.digitalocean.com/products/app-platform/how-to/manage-databases/)
- [Environment Variables](https://docs.digitalocean.com/products/app-platform/how-to/use-environment-variables/)

## 🔐 Безопасность

- Используйте тип `SECRET` для чувствительных переменных окружения
- Никогда не коммитьте `.env` файлы в репозиторий
- Используйте разные API ключи для development и production

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в DigitalOcean Console
2. Убедитесь что все переменные окружения установлены
3. Проверьте документацию DigitalOcean

---

**Готово к деплою!** 🚀

После деплоя ваше приложение будет доступно 24/7 с автоматическими обновлениями при каждом изменении кода.
