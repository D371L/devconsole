# 🔧 Исправление ошибки деплоя: "determine start command"

## Проблема

Ошибка: `ERROR: failed to launch: determine start command: when there is no default process a command is required`

Это означает, что DigitalOcean пытается запустить приложение как **Web Service** (требует команду запуска), но это должен быть **Static Site** (просто отдает файлы).

## ✅ Решение (5 минут)

### Шаг 1: Откройте настройки компонента

1. Зайдите в [DigitalOcean App Platform](https://cloud.digitalocean.com/apps)
2. Откройте ваше приложение **devconsole**
3. Перейдите в раздел **Settings** (в левом меню)
4. Прокрутите до секции **Components**

### Шаг 2: Измените тип компонента

1. Найдите компонент с типом **Web Service** (с красным восклицательным знаком)
2. Нажмите на **Edit** (или иконку карандаша) рядом с компонентом
3. В открывшемся окне найдите поле **Component Type** или **Type**
4. Измените с **Web Service** на **Static Site**
5. Убедитесь что настройки:
   - **Build Command**: `npm install && npm run build`
   - **Output Directory**: `dist`
   - **HTTP Port**: **ОСТАВЬТЕ ПУСТЫМ** (важно!)
   - **Routes**: `/` (или оставьте по умолчанию)
6. Нажмите **Save** или **Update**

### Шаг 3: Запустите новый деплой

1. После сохранения DigitalOcean автоматически запустит новый деплой
2. Или нажмите **Deploy** вручную
3. Дождитесь завершения (2-3 минуты)

## Альтернативный вариант: Удалить и пересоздать

Если изменение типа не работает:

1. **Удалите** существующий компонент Web Service
2. Нажмите **Add Component**
3. Выберите **Static Site**
4. Настройте:
   - **Source**: GitHub → ваш репозиторий → ветка `main`
   - **Build Command**: `npm install && npm run build`
   - **Output Directory**: `dist`
   - **HTTP Port**: оставьте пустым
5. Добавьте переменную окружения `GEMINI_API_KEY` (если еще не добавлена)
6. Сохраните и задеплойте

## Проверка после исправления

После изменения типа на Static Site:
- ✅ Build должен завершиться успешно
- ✅ Deploy должен пройти без ошибок
- ✅ Не будет ошибки "determine start command"
- ✅ Приложение будет доступно по URL

## Почему это происходит?

DigitalOcean автоматически определяет тип приложения:
- Если видит `package.json` с Node.js → может создать **Web Service**
- Для статического сайта нужен тип **Static Site**

Статический сайт не требует запуска процесса - просто отдает HTML/CSS/JS файлы.

## Если проблема сохраняется

1. Убедитесь что **HTTP Port** оставлен пустым
2. Проверьте что **Output Directory** указан как `dist`
3. Убедитесь что в `dist` есть файлы после сборки
4. Проверьте логи Build - сборка должна быть успешной

